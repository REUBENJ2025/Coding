import random

def main():
    choices = ["Lukas", "Reuben", "Pavel"]
    probabilities = [0.7, 0.2, 0.1]  

    result = random.choices(choices, weights=probabilities, k=1)[0]
    print(result)


main()